#include <stdio.h>

/**
 *main-this function prints out the alphabet
 *Return: It returns zero
 */
int main(void)
{
int count = 0;
while (count <= 15)
{
printf("%x", count);
count++;
}
printf("\n");
return (0);
}
