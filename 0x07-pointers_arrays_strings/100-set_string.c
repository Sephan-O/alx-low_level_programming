#include "main.h"

/**
 * set_string - A func that sets the value of a pointer to a char
 * @s: source parameter.
 * @to: target parameter.
 * Return: voids don't return anything
 */

void set_string(char **s, char *to)

{

		*s = to;

}
