#include <stdio.h>

/**
 * main - func that prints the program's name
 * @argc: parameters supplied through the command line
 * @argv: array of character pointer, which contains all the parameters.
 * Return: 0
 */
int main(int argc, char *argv[])
{
printf("%s\n",argv[0]);
return (0);
}
